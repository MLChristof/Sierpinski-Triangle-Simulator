
export interface Point {
  x: number;
  y: number;
}

export interface Viewport {
  x: number; // world x-coordinate at the center of the canvas
  y: number; // world y-coordinate at the center of the canvas
  scale: number;
}
